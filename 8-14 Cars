def car_builder(manufacturer, model_name, **car_info):
    car_info['car_manufacturer'] = manufacturer
    car_info['car_model_name'] = model_name
    return car_info
car = car_builder('Subaru', 'outback', color = 'red', tow_package = True)
print(car)
